[project]
name = "muffin-worker-agent-execution"
version = "0.1.0"
description = "Not implemented yet - scaffolded alongside worker/document_process, see its README"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
