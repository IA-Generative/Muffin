{{- if .Values.enabled }}
{{- if or .Values.agent_execution.envCm .Values.global.envCm -}}
kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "agent_execution")) | nindent 4 }}
data:
  {{- if .Values.global.envCm -}}
  {{- include "helper.env" (dict "root" . "data" .Values.global.envCm) | indent 2 }}
  {{- end -}}
  {{- if .Values.agent_execution.envCm -}}
  {{- include "helper.env" (dict "root" . "data" .Values.agent_execution.envCm) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
