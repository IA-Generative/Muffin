{{- if .Values.enabled }}
{{- /* Gated on `rules` as well, exactly like clusterrole.yaml: without it, an empty `rules` list
yields a ClusterRoleBinding whose roleRef points at a ClusterRole that was never created. */ -}}
{{- if and .Values.agent_execution.serviceAccount.enabled .Values.agent_execution.serviceAccount.clusterRole.create .Values.agent_execution.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "agent_execution")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.agent_execution.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "agent_execution")) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
