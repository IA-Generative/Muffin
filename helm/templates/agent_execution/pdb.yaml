{{- if .Values.enabled }}
{{- if .Values.agent_execution.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "agent_execution")) | nindent 4 }}
  {{- if .Values.agent_execution.pdb.labels }}
    {{- toYaml .Values.agent_execution.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.agent_execution.pdb.annotations }}
  annotations: {{- toYaml .Values.agent_execution.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Tested against the empty string rather than for truthiness: `maxUnavailable: 0` is a valid
  setting (it blocks every voluntary eviction) but is falsy, so `with`/`if` would silently fall
  through to minAvailable. `toString` maps both nil and "" to "", which are the two unset forms.
  `helper.component.validate` guarantees at least one of the two is set. */ -}}
  {{- if ne (toString .Values.agent_execution.pdb.maxUnavailable) "" }}
  maxUnavailable: {{ .Values.agent_execution.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ .Values.agent_execution.pdb.minAvailable }}
  {{- end }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "agent_execution") | nindent 6 }}
{{- end }}
{{- end }}
