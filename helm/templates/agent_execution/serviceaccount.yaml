{{- if .Values.enabled }}
{{- if and .Values.agent_execution.serviceAccount.enabled .Values.agent_execution.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.agent_execution.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "agent_execution")) }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "agent_execution")) | nindent 4 }}
  {{- if .Values.agent_execution.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.agent_execution.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.agent_execution.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
