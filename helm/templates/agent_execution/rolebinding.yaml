{{- if .Values.enabled }}
{{- /* Gated on `rules` as well, exactly like role.yaml: without it, an empty `rules` list yields a
RoleBinding whose roleRef points at a Role that was never created. */ -}}
{{- if and .Values.agent_execution.serviceAccount.enabled .Values.agent_execution.serviceAccount.role.create .Values.agent_execution.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "agent_execution")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.agent_execution.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "agent_execution")) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
