{{- if .Values.enabled }}
{{- if .Values.agent_execution.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "agent_execution") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "agent_execution")) | nindent 4 }}
  {{- if .Values.agent_execution.networkPolicy.labels }}
    {{- toYaml .Values.agent_execution.networkPolicy.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.agent_execution.networkPolicy.annotations }}
  annotations: {{- toYaml .Values.agent_execution.networkPolicy.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Required: an omitted podSelector defaults to `{}`, which would apply these rules to EVERY
  pod in the release namespace instead of just this component's pods. */}}
  podSelector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "agent_execution") | nindent 6 }}
  policyTypes: {{- toYaml .Values.agent_execution.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.agent_execution.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.agent_execution.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}
