{{- if .Values.enabled }}
{{- if .Values.document_process.ingress.enabled -}}
{{- $fullNameApp := include "helper.componentFullname" (dict "root" . "componentName" "document_process") -}}
{{- $svcPortApp := .Values.document_process.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
  {{- if .Values.document_process.ingress.labels }}
    {{- toYaml .Values.document_process.ingress.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.document_process.ingress.annotations }}
  annotations:
    {{- tpl (toYaml .Values.document_process.ingress.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.document_process.ingress.className }}
  ingressClassName: {{ .Values.document_process.ingress.className }}
  {{- end }}
  {{- if gt (len .Values.document_process.ingress.tls) 0 }}
  tls:
  {{- range .Values.document_process.ingress.tls }}
  - hosts:
    {{- range .hosts }}
    - {{ . | quote }}
    {{- end }}
    secretName: {{ .secretName | default (printf "%s-%s" $fullNameApp "tls") | quote }}
  {{- end }}
  {{- end }}
  rules:
  {{- range .Values.document_process.ingress.hosts }}
  - host: {{ .name | quote }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ .path | default "/" }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
          {{- if .backend }}
            name: {{ .backend.serviceName | default $fullNameApp }}
            port:
              number: {{ .backend.portNumber | default $svcPortApp }}
          {{- else }}
            name: {{ $fullNameApp }}
            port:
              number: 80
          {{- end }}
      {{- end }}
  {{- end }}
{{- end }}
{{- end }}
