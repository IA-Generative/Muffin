{{- if .Values.enabled }}
{{- if .Values.document_process.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
  {{- if .Values.document_process.networkPolicy.labels }}
    {{- toYaml .Values.document_process.networkPolicy.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.document_process.networkPolicy.annotations }}
  annotations: {{- toYaml .Values.document_process.networkPolicy.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Required: an omitted podSelector defaults to `{}`, which would apply these rules to EVERY
  pod in the release namespace instead of just this component's pods. */}}
  podSelector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "document_process") | nindent 6 }}
  policyTypes: {{- toYaml .Values.document_process.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.document_process.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.document_process.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}
