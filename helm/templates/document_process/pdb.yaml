{{- if .Values.enabled }}
{{- if .Values.document_process.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
  {{- if .Values.document_process.pdb.labels }}
    {{- toYaml .Values.document_process.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.document_process.pdb.annotations }}
  annotations: {{- toYaml .Values.document_process.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Tested against the empty string rather than for truthiness: `maxUnavailable: 0` is a valid
  setting (it blocks every voluntary eviction) but is falsy, so `with`/`if` would silently fall
  through to minAvailable. `toString` maps both nil and "" to "", which are the two unset forms.
  `helper.component.validate` guarantees at least one of the two is set. */ -}}
  {{- if ne (toString .Values.document_process.pdb.maxUnavailable) "" }}
  maxUnavailable: {{ .Values.document_process.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ .Values.document_process.pdb.minAvailable }}
  {{- end }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "document_process") | nindent 6 }}
{{- end }}
{{- end }}
