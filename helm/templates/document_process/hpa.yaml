{{- if .Values.enabled }}
{{- /* DaemonSet is excluded on purpose: it has no scale subresource, so an HPA targeting one is
rejected by the API server. `helper.component.validate` already fails on that combination - this
guard just keeps the template honest if the validation is ever relaxed. */ -}}
{{- if and .Values.document_process.autoscaling.enabled (ne .Values.document_process.deploymentType "DaemonSet") }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.document_process.deploymentType }}
    name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  minReplicas: {{ .Values.document_process.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.document_process.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.document_process.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.document_process.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.document_process.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.document_process.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
{{- end }}
