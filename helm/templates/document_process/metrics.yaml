{{- if .Values.enabled }}
{{- if .Values.document_process.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process-metrics") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process-metrics")) | nindent 4 }}
  {{- if .Values.document_process.metrics.service.labels }}
    {{- toYaml .Values.document_process.metrics.service.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.document_process.metrics.service.annotations }}
  annotations: {{- toYaml .Values.document_process.metrics.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.document_process.metrics.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.document_process.metrics.service.port }}
    targetPort: {{ .Values.document_process.metrics.service.targetPort }}
    protocol: TCP
    name: {{ .Values.document_process.metrics.service.portName | default "metrics" }}
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "document_process") | nindent 4 }}
{{- end }}
{{- end }}
