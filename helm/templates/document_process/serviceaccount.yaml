{{- if .Values.enabled }}
{{- if and .Values.document_process.serviceAccount.enabled .Values.document_process.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.document_process.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "document_process")) }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
  {{- if .Values.document_process.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.document_process.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.document_process.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
