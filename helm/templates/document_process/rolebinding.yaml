{{- if .Values.enabled }}
{{- /* Gated on `rules` as well, exactly like role.yaml: without it, an empty `rules` list yields a
RoleBinding whose roleRef points at a Role that was never created. */ -}}
{{- if and .Values.document_process.serviceAccount.enabled .Values.document_process.serviceAccount.role.create .Values.document_process.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.document_process.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "document_process")) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
