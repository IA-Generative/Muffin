{{- if .Values.enabled }}
{{- if or .Values.document_process.envSecret .Values.global.envSecret -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "document_process") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "document_process")) | nindent 4 }}
data:
  {{- if .Values.global.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.global.envSecret) | indent 2 }}
  {{- end -}}
  {{- if .Values.document_process.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.document_process.envSecret) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
