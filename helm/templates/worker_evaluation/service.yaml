{{- if .Values.enabled }}
{{- /* Not gated on `deploymentType`: every supported kind (Deployment/StatefulSet/DaemonSet) can
sit behind a Service, and `service.enabled` is the switch for components that take no traffic.
This matches metrics.yaml, which is gated the same way. */ -}}
{{- if .Values.worker_evaluation.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
spec:
  type: {{ .Values.worker_evaluation.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.worker_evaluation.service.port | default 80 }}
    targetPort: {{ .Values.worker_evaluation.containerPortName | default .Values.worker_evaluation.containerPort | default 8080 }}
    protocol: {{ .Values.worker_evaluation.service.protocol | default "TCP" }}
    {{- if and (eq .Values.worker_evaluation.service.type "NodePort") .Values.worker_evaluation.service.nodePort }}
    nodePort: {{ .Values.worker_evaluation.service.nodePort }}
    {{- end }}
    name: {{ .Values.worker_evaluation.service.portName | default "http" }}
  {{- if .Values.worker_evaluation.service.extraPorts -}}
  {{- toYaml .Values.worker_evaluation.service.extraPorts | nindent 2 -}}
  {{- end }}
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "worker_evaluation") | nindent 4 }}
{{- end }}
{{- end }}
