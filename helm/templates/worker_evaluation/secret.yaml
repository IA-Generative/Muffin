{{- if .Values.enabled }}
{{- if or .Values.worker_evaluation.envSecret .Values.global.envSecret -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
data:
  {{- if .Values.global.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.global.envSecret) | indent 2 }}
  {{- end -}}
  {{- if .Values.worker_evaluation.envSecret -}}
  {{- include "helper.secret" (dict "root" . "data" .Values.worker_evaluation.envSecret) | indent 2 }}
  {{- end -}}
{{- end -}}
{{- end }}
