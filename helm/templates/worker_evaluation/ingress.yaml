{{- if .Values.enabled }}
{{- if .Values.worker_evaluation.ingress.enabled -}}
{{- $fullNameApp := include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") -}}
{{- $svcPortApp := .Values.worker_evaluation.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
  {{- if .Values.worker_evaluation.ingress.labels }}
    {{- toYaml .Values.worker_evaluation.ingress.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.worker_evaluation.ingress.annotations }}
  annotations:
    {{- tpl (toYaml .Values.worker_evaluation.ingress.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.worker_evaluation.ingress.className }}
  ingressClassName: {{ .Values.worker_evaluation.ingress.className }}
  {{- end }}
  {{- if gt (len .Values.worker_evaluation.ingress.tls) 0 }}
  tls:
  {{- range .Values.worker_evaluation.ingress.tls }}
  - hosts:
    {{- range .hosts }}
    - {{ . | quote }}
    {{- end }}
    secretName: {{ .secretName | default (printf "%s-%s" $fullNameApp "tls") | quote }}
  {{- end }}
  {{- end }}
  rules:
  {{- range .Values.worker_evaluation.ingress.hosts }}
  - host: {{ .name | quote }}
    http:
      paths:
      {{- range .paths }}
      - path: {{ .path | default "/" }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
          {{- if .backend }}
            name: {{ .backend.serviceName | default $fullNameApp }}
            port:
              number: {{ .backend.portNumber | default $svcPortApp }}
          {{- else }}
            name: {{ $fullNameApp }}
            port:
              number: 80
          {{- end }}
      {{- end }}
  {{- end }}
{{- end }}
{{- end }}
