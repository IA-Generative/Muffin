{{- if .Values.enabled }}
{{- if and .Values.worker_evaluation.serviceAccount.enabled .Values.worker_evaluation.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.worker_evaluation.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation")) }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
  {{- if .Values.worker_evaluation.serviceAccount.annotations }}
  annotations:
    {{- tpl (toYaml .Values.worker_evaluation.serviceAccount.annotations) . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.worker_evaluation.serviceAccount.automountServiceAccountToken }}
{{- end }}
{{- end }}
