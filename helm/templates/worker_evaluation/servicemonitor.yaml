{{- if .Values.enabled }}
{{- if and .Values.worker_evaluation.metrics.enabled .Values.worker_evaluation.metrics.serviceMonitor.enabled }}
{{- $metricSvcPort := .Values.worker_evaluation.metrics.service.portName | default .Values.worker_evaluation.metrics.service.port -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation-metrics") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
  {{- if .Values.worker_evaluation.metrics.serviceMonitor.labels }}
    {{- toYaml .Values.worker_evaluation.metrics.serviceMonitor.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.worker_evaluation.metrics.serviceMonitor.annotations }}
  annotations: {{- toYaml .Values.worker_evaluation.metrics.serviceMonitor.annotations | nindent 4 }}
  {{- end }}
spec:
  endpoints:
  {{- range .Values.worker_evaluation.metrics.serviceMonitor.endpoints }}
  - port: {{ $metricSvcPort }}
    path: {{ .path }}
    honorLabels: {{ .honorLabels }}
    {{- if and .basicAuth .basicAuth.username .basicAuth.password }}
    basicAuth: {{- toYaml .basicAuth | nindent 6 }}
    {{- end }}
    {{- if and .bearerTokenSecret .bearerTokenSecret.name .bearerTokenSecret.key }}
    bearerTokenSecret: {{- toYaml .bearerTokenSecret | nindent 6 }}
    {{- end }}
    {{- if .interval }}
    interval: {{ .interval }}
    {{- end }}
    {{- if .scrapeTimeout }}
    scrapeTimeout: {{ .scrapeTimeout }}
    {{- end }}
    {{- if .relabelings }}
    relabelings: {{- toYaml .relabelings | nindent 6 }}
    {{- end }}
    {{- if .metricRelabelings }}
    metricRelabelings: {{- toYaml .metricRelabelings | nindent 6 }}
    {{- end }}
    {{- if .scheme }}
    scheme: {{ .scheme }}
    {{- end }}
    {{- if .tlsConfig }}
    tlsConfig: {{- toYaml .tlsConfig | nindent 6 }}
    {{- end }}
  {{- end }}
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace | quote }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "worker_evaluation-metrics") | nindent 6 }}
{{- end }}
{{- end }}
