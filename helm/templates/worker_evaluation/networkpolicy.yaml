{{- if .Values.enabled }}
{{- if .Values.worker_evaluation.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
  {{- if .Values.worker_evaluation.networkPolicy.labels }}
    {{- toYaml .Values.worker_evaluation.networkPolicy.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.worker_evaluation.networkPolicy.annotations }}
  annotations: {{- toYaml .Values.worker_evaluation.networkPolicy.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- /* Required: an omitted podSelector defaults to `{}`, which would apply these rules to EVERY
  pod in the release namespace instead of just this component's pods. */}}
  podSelector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "worker_evaluation") | nindent 6 }}
  policyTypes: {{- toYaml .Values.worker_evaluation.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.worker_evaluation.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.worker_evaluation.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}
