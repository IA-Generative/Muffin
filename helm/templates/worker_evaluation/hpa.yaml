{{- if .Values.enabled }}
{{- /* DaemonSet is excluded on purpose: it has no scale subresource, so an HPA targeting one is
rejected by the API server. `helper.component.validate` already fails on that combination - this
guard just keeps the template honest if the validation is ever relaxed. */ -}}
{{- if and .Values.worker_evaluation.autoscaling.enabled (ne .Values.worker_evaluation.deploymentType "DaemonSet") }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: {{ .Values.worker_evaluation.deploymentType }}
    name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  minReplicas: {{ .Values.worker_evaluation.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.worker_evaluation.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.worker_evaluation.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.worker_evaluation.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.worker_evaluation.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.worker_evaluation.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
{{- end }}
