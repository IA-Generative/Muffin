{{- if .Values.enabled }}
{{- /* Gated on `rules` as well, exactly like role.yaml: without it, an empty `rules` list yields a
RoleBinding whose roleRef points at a Role that was never created. */ -}}
{{- if and .Values.worker_evaluation.serviceAccount.enabled .Values.worker_evaluation.serviceAccount.role.create .Values.worker_evaluation.serviceAccount.role.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "worker_evaluation")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation") }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.worker_evaluation.serviceAccount.name | default (include "helper.componentFullname" (dict "root" . "componentName" "worker_evaluation")) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}
