server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: let Vue Router (history mode) own client-side paths.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
